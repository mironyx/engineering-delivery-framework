# File Map

Where things live in this project. Referenced by EDF skills.

| Concept | Path |
|---|---|
| engine-dir | <!-- e.g. `src/lib/engine/` --> |
| api-dir | <!-- e.g. `src/app/api/` --> |
| types-dir | <!-- e.g. `src/types/` --> |
| test-dir | <!-- e.g. `tests/` --> |
| composition-root | <!-- e.g. `src/lib/api/context.ts` --> |
| db-types | <!-- e.g. `src/types/database.types.ts` --> |
| schema-dir | <!-- e.g. `supabase/schemas/` if declarative; blank for hand-authored migrations --> |
| migration-dir | <!-- e.g. `supabase/migrations/` (generated) or `db/migrate/` (hand-authored) --> |
