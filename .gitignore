.claude/
.claude-plugin/cache/
__pycache__/

.diagnostics
.vs
.sonar/

# Repo-root docs/ — plugin docs live under plugins/edf/docs/
/docs/

# Environment
.env

# Dependencies & build output
node_modules/
out/
.vscode-test/
# VSIX artifacts ship WITH the plugin via plugins/edf/artifacts/ — they must be
# committed, so ignore stray build outputs but keep the artifacts dir tracked.
*.vsix
!plugins/edf/artifacts/*.vsix
